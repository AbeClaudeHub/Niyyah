{
  "name": "niyyah-functions",
  "description": "Cloud Functions for Niyyah — Stripe checkout, billing portal, and subscription webhook",
  "private": true,
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log --only createCheckoutSession,createPortalSession,stripeWebhook"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1",
    "stripe": "^17.5.0"
  }
}
